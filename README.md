# Micronsoft-POS
Full-stack POS system built with Django REST + React + MySQL. Covers high-concurrency stock management (SELECT FOR UPDATE), big data analytics over 100k records (&lt;500ms), and transactional POS checkout with atomic integrity.
